# Claude Code 開発ガイドライン

## コーディング原則

### KISS (Keep It Simple, Stupid)
シンプルさを保つことを最優先とする原則。複雑な設計よりも、理解しやすく保守しやすいコードを書くことを心がける。

**常に意識すること：**
- 不必要な抽象化や複雑な設計パターンを避ける
- コードは読みやすさを優先し、明確で直感的な実装を選択する
- 過度な最適化や将来の拡張を見越した設計は避ける

### YAGNI (You Aren't Gonna Need It)
今必要でない機能は実装しない原則。将来必要になるかもしれない機能のために、現時点で実装しない。

**常に意識すること：**
- 現在の要件にのみ焦点を当てる
- 仮定の将来の要件のためのコードを書かない
- 必要になったタイミングで実装する

### DRY (Don't Repeat Yourself)
同じコードや知識を重複させない原則。コードの重複は保守性を下げるため、共通化可能な部分は適切に抽象化する。

**常に意識すること：**
- 重複したコードパターンを見つけたら、適切に共通化する
- ただし、過度な抽象化は避ける（KISSとのバランス）
- 真に同じ概念である場合のみ共通化する

---

## MCP サーバー活用ガイド（必須）

MCPサーバーは積極的に活用する。特にserenaとcontext7は必須ツールとして扱う。

### Serena（常に使用）
コードベースを効率的に探索・編集するためのセマンティックコーディングツール。

**必須事項：**
- コード探索・編集時は常にserenaを使用する
- ファイル全体を読む前に、`get_symbols_overview`でシンボルの概要を把握
- コード検索には`find_symbol`や`search_for_pattern`を活用
- シンボル単位での編集ツール（`replace_symbol_body`等）を優先使用

### Context7（コード記述前に必須）
最新のライブラリドキュメントとコード例を取得するツール。

**必須事項：**
- **コードを記述する前に必ず使用する**
- 言語や技術スタックの最新ドキュメントを参照し、公式の推奨に従う
- 最新の記述方式に従い、**非推奨や以前の記述方法は使用しない**
- エラー発生時にも公式ドキュメントを確認して正しい解決策を見つける
- まず`resolve-library-id`でライブラリIDを取得してから`query-docs`を使用

### shadcn/ui
UIコンポーネントの検索、追加、実装例の参照ツール。

**使用時の注意：**
- プロジェクトにcomponents.jsonが必要（ない場合はinit_projectを使用）
- コンポーネントは`search_items_in_registries`で検索してから詳細を確認
- 使用例は`get_item_examples_from_registries`で完全な実装コードを取得

---

## 開発環境

### mise（開発環境管理）
複数言語・ツールのバージョン管理を統一的に行う。

**必須事項：**
- 開発環境のセットアップには`mise`を使用
- `.mise.toml`でプロジェクトのツールバージョンを管理

### uv（Python環境管理）
高速なPythonパッケージマネージャー。

**必須事項：**
- Pythonのバージョン管理には`uv`を使用
- ライブラリの追加には`uv add`を使用
- **`pip`は使用禁止**（`pip install`ではなく`uv add`）
- 依存関係は`pyproject.toml`で管理

```bash
# 正しい例
uv add fastapi
uv add sqlalchemy

# 禁止例
pip install fastapi  # ← 使用禁止
```

---

## 型指定ルール

### Any型使用禁止

**必須事項：**
- `typing.Any`の使用は禁止
- 具体的な型、Union型、Genericsを使用して厳密に型付けする
- 型が不明な場合は適切な型を調査・定義する

```python
# 禁止例
def process(data: Any) -> Any:  # ← 使用禁止
    ...

# 正しい例
def process(data: dict[str, str]) -> ProcessResult:
    ...
```

---

## コーディングワークフロー

### コード記述前
1. **Context7で最新ドキュメントを参照**（必須）
2. 公式推奨のベストプラクティスを確認
3. 最新の記述方式を把握

### コード記述後（必須チェック）
以下を必ず実行し、**全てパスするまで修正する（手抜き禁止）**：

1. **コードフォーマット** - 統一されたスタイルに整形
2. **型チェック** - 型エラーがないことを確認
3. **テスト実行** - 全テストがパスすることを確認

### 完了時
- 全てのチェックがパスしたら**git pushまで実行**する

---

## 言語使用ルール

- **思考プロセスとコード記述**: 英語
- **チャット対応とコメント**: 日本語
