[project]
name = "adms-api"
version = "0.1.0"
description = "ADMS - Advanced Drone Management System API"
readme = "README.md"
license = { text = "Proprietary" }
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]",
    "uvicorn[standard]",
    "pydantic>=2",
    "pydantic-settings",
    "sqlalchemy>=2",
    "asyncpg",
    "alembic",
    "redis",
    "argon2-cffi",
    "python-jose[cryptography]",
    "httpx",
    "tzdata>=2025.3",
    "greenlet>=3.3.0",
]

[dependency-groups]
dev = [
    "pytest",
    "pytest-asyncio",
    "pytest-cov",
    "ruff",
    "pyright",
    "pre-commit",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.ruff]
target-version = "py314"
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = [
    "E",      # pycodestyle errors
    "W",      # pycodestyle warnings
    "F",      # Pyflakes
    "I",      # isort
    "B",      # flake8-bugbear
    "C4",     # flake8-comprehensions
    "UP",     # pyupgrade
    "ARG",    # flake8-unused-arguments
    "SIM",    # flake8-simplify
    "TCH",    # flake8-type-checking
    "PTH",    # flake8-use-pathlib
    "ERA",    # eradicate
    "PL",     # Pylint
    "RUF",    # Ruff-specific rules
]
ignore = [
    "PLR0913",  # Too many arguments
    "PLR2004",  # Magic value comparison
]

[tool.ruff.lint.per-file-ignores]
# SQLAlchemy models need runtime access to types for mapped_column
"src/models/*.py" = ["TCH003"]
"src/db/*.py" = ["TCH003"]
# AsyncGenerator is used in runtime type annotation for lifespan
"src/main.py" = ["TCH003"]
# FastAPI dependencies and routers need runtime type access
"src/dependencies/*.py" = ["TCH001", "TCH002", "TCH003"]
"src/routers/*.py" = ["TCH001", "TCH002", "TCH003"]
# Pydantic schemas need runtime type access
"src/schemas/*.py" = ["TCH001", "TCH003"]
# Services need runtime type access
"src/services/*.py" = ["TCH001", "TCH002", "TCH003"]

[tool.ruff.lint.isort]
known-first-party = ["src"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.pyright]
pythonVersion = "3.14"
typeCheckingMode = "strict"
reportMissingImports = true
reportMissingTypeStubs = false
reportUnusedImport = true
reportUnusedVariable = true
reportUnnecessaryTypeIgnoreComment = true

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "-v --cov=src --cov-report=term-missing"
